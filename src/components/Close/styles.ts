import styled from 'styled-components';

export const ContainerHeader = styled.div`
    position: relative;
    text-align: end;
    top: 5rem;
    right: 5rem;

   > button {
        background: transparent;
    }
`;
